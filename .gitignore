# 密钥与本地配置（密钥只放 .env，RULES §3.1）
.env
.env.*
!.env.example
config/settings.yaml
config/settings.local.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# 测试与质量
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
reports/

# 运行时数据（SQLite 默认存放 + 动态工作目录）
data/
.workspace/
allure-results/
allure-report/

# 日志
logs/
*.log

# IDE
.idea/
.vscode/
*.swp

# ---- 以下只保留项目本体，不公开（已上传文件用 git rm --cached 移出跟踪）----

# Claude Code / AI 协作（规则/技能/指南/记忆，仅本地开发用）
.claude/
CLAUDE.md
memory.md

# 文档中的对话产物（过程记录，不公开；只保留设计/架构/数据库等文档）
docs/plans/
docs/reviews/
docs/sessions/
docs/session-doc-template.md
